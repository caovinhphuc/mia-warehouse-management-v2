[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# SPA routing - only redirect if file doesn't exist (force = false)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
