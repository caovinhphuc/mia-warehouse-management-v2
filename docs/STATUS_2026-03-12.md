# Tình Trạng Dự Án — 12/03/2026

## 1. Tổng Quan

| Hạng mục | Giá trị |
|---|---|
| Repo | `caovinhphuc/mia-warehouse-management-v2` |
| Branch | `main` |
| Last commit | `6fe678fcd` — fix broken script refs + add command docs |
| Node | v22.22.1 (fnm, `.nvmrc`) |
| npm | 10.9.4 |
| Build tool | Vite 7.3.1 |
| Frontend port | 3000 |
| Backend port | 3001 |
| AI Service port | 8000 (chưa chạy) |

---

## 2. Trạng Thái Services (Health Check)

| Service | Status | Ghi chú |
|---|---|---|
| **Google Sheets** | ✅ healthy | Connected — 121 sheets trong "mia-logistics-final" |
| **Google Drive** | ✅ healthy | Connected — listFiles working |
| **Auth** | ✅ healthy | Login OK (`admin@mia.vn` / `admin123`) |
| **Audit** | ✅ healthy | Available |
| **AI** | ✅ healthy | Available (endpoint sẵn, AI Service chưa chạy) |
| **Alerts** | ✅ healthy | Available |
| **Script** | ✅ healthy | Available |
| **Automation** | ✅ healthy | Available |
| **Custom Metrics** | ✅ healthy | Available |
| **Retail Metrics** | ✅ healthy | Available |
| **Email (SendGrid)** | ⚠️ warning | Not configured — thiếu `SENDGRID_API_KEY` |
| **Telegram** | ⚠️ warning | 404 — Bot token chưa đúng/chưa config |
| **Google Apps Script** | ⚠️ warning | Not configured (optional) |
| **AI Service (Python)** | ❌ off | Chưa start — cần `uvicorn` trên port 8000 |

---

## 3. Đã Hoàn Thành (Session 12/03)

### Git & Cleanup

- [x] Git hygiene: 119,068 → 921 tracked files (99.2% reduction)
- [x] `.gitignore` sạch — node_modules, .env, backup, build excluded
- [x] Merge `cleanup/git-hygiene` → `main`, push GitHub

### Code Fixes

- [x] Port 5 code improvements từ warehouse-backup (authRoutes, driveRoutes, vite proxy, googleDriveApi, googleSheetsApi)
- [x] Fix `importMetaEnv.js` operator precedence (`??` vs `||`)
- [x] Remove `react-scripts` → vulnerabilities 26 → 6
- [x] Rename `vite.config.js` → `vite.config.mjs` (fix ERR_REQUIRE_ESM)
- [x] `.nvmrc` 18 → 22 (fix Vite 7 `crypto.hash is not a function`)
- [x] Fix 15 broken script references trong `package.json` (42/42 OK)
- [x] **Fix backend port**: `server.js` load `backend/.env` trước root `.env` → đúng port 3001

### Infrastructure

- [x] Port 12 infra files từ backup (docker-compose, deploy scripts, monitoring, setup)
- [x] Copy `service-account-key.json` vào `backend/config/`
- [x] Tạo `backend/.env` với giá trị thật
- [x] Update root `.env` — Google Sheets/Drive values thật

### Documentation

- [x] `docs/COMMANDS.md` — tất cả npm scripts
- [x] `docs/SCRIPTS_INFRASTRUCTURE.md` — scripts structure, ports, Docker

### Verification

- [x] Build PASS (Vite, 888 modules)
- [x] Dev server PASS (Vite v7.3.1, 224ms)
- [x] Backend port 3001 ✅
- [x] Frontend port 3000 ✅
- [x] Google Sheets API ✅
- [x] Google Drive API ✅
- [x] Login flow ✅ (verify-one-tga skip trong dev → login OK)
- [x] Vite proxy `/api` → `localhost:3001` ✅

---

## 4. Chưa Commit (Cần Xử Lý)

| File | Thay đổi | Action |
|---|---|---|
| `backend/server.js` | Fix dotenv load order (port 3001) | **Commit** |
| `backend/.env` | Chứa credentials thật | **KHÔNG commit** — đã trong .gitignore |
| `backend/config/service-account-key.json` | Google SA credentials | **KHÔNG commit** — đã trong .gitignore |
| `.env` (root) | Updated Google values | **KHÔNG commit** — đã trong .gitignore |
| `.claude/` | Claude Code config (untracked) | Tùy chọn — `.gitignore` hoặc commit |
| `.cursor/` | Cursor IDE config (untracked) | `.gitignore` |
| `pnpm-lock.yaml` | Không dùng (project dùng npm) | **Xóa** |

---

## 5. Vấn Đề Cần Xử Lý (TODO Ngày Mai)

### 🔴 Ưu tiên cao

#### 5.1 AI Service chưa chạy

- Thư mục `ai-service/` có sẵn code (`ai_service.py`, `requirements.txt`, `Dockerfile.ai`)
- Backend `.env` set `AI_SERVICE_URL=http://localhost:5000` nhưng code fallback = `8000`
- **Fix**: Thống nhất port 8000, install dependencies, start service

```bash
cd ai-service
pip install -r requirements.txt
python -m uvicorn ai_service:app --port 8000
```

- Sau đó update `backend/.env`: `AI_SERVICE_URL=http://localhost:8000`

#### 5.2 Commit `backend/server.js` fix

```bash
git add backend/server.js
git commit -m "fix: backend load backend/.env first to get correct PORT=3001"
git push
```

#### 5.3 Xóa `pnpm-lock.yaml`

- Project dùng npm (packageManager: `npm@9.9.1`)
- `pnpm-lock.yaml` tồn tại nhưng không dùng → gây confuse

```bash
git rm pnpm-lock.yaml
git commit -m "chore: remove unused pnpm-lock.yaml (project uses npm)"
git push
```

### 🟡 Ưu tiên trung bình

#### 5.4 Fix broken test scripts (craco/react-scripts removed)

10 scripts trong `package.json` vẫn reference `craco` hoặc `react-scripts`:

- `build:analyze`, `build:no-sourcemap`, `build:minimal` → craco
- `test:watch` → craco
- `test:coverage`, `test:unit` → react-scripts
- `analyze`, `analyze:webpack`, `analyze:webpack:interactive`, `analyze:all` → craco

**Fix**: Migrate sang Vite equivalents hoặc xóa scripts không cần.

#### 5.5 Root `.env` còn 2 placeholder values

```
VITE_GOOGLE_SHEETS_API_KEY=your_google_api_key_here
VITE_GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
```

- Kiểm tra xem frontend có dùng không, nếu có thì điền giá trị thật

#### 5.6 npm vulnerabilities (6 remaining)

```
6 vulnerabilities (4 low, 2 high)
- elliptic / crypto-browserify (high)
- serialize-javascript / compression-webpack-plugin (low)
```

- `npm audit fix --force` có thể breaking → cần test sau khi fix

#### 5.7 SendGrid Email chưa config

- Cần `SENDGRID_API_KEY` trong `.env`
- Hoặc tắt email service nếu không dùng

#### 5.8 Telegram Bot chưa config đúng

- Cần kiểm tra `TELEGRAM_BOT_TOKEN` và `TELEGRAM_CHAT_ID`
- Health check trả 404

### 🟢 Ưu tiên thấp

#### 5.9 Google Apps Script (optional)

- Chưa config — chỉ cần nếu dùng GAS automation

#### 5.10 `.claude/` và `.cursor/` cleanup

- Thêm vào `.gitignore` nếu không muốn track

#### 5.11 Backend `AI_SERVICE_URL` không thống nhất

- `backend/.env`: `http://localhost:5000`
- Root `.env`: `AI_SERVICE_PORT=8000`
- Code fallback: `http://localhost:8000`
- **Fix**: Thống nhất tất cả về port 8000

---

## 6. Lệnh Chạy Nhanh

```bash
# Start backend (port 3001)
cd backend && node server.js

# Start frontend (port 3000)
npx vite --port 3000

# Start AI Service (port 8000) — chưa test
cd ai-service && pip install -r requirements.txt && python -m uvicorn ai_service:app --port 8000

# Health check
curl -s http://localhost:3001/health | python3 -m json.tool

# Test login
curl -s -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@mia.vn","password":"admin123"}'

# Test Google Sheets
curl -s http://localhost:3001/api/sheets/metadata | python3 -m json.tool

# Test Google Drive
curl -s "http://localhost:3001/api/drive/files?pageSize=3" | python3 -m json.tool
```

---

## 7. Cấu Trúc Ports

| Service | Port | Status |
|---|---|---|
| Frontend (Vite) | 3000 | ✅ Running |
| Backend (Express) | 3001 | ✅ Running |
| AI Service (FastAPI) | 8000 | ❌ Not started |
| WebSocket (Socket.io) | 3001 (shared) | ✅ Running |
| Native WebSocket | 3001/ws | ✅ Running |

---

## 8. Credentials Quan Trọng (KHÔNG commit)

| File | Nội dung |
|---|---|
| `.env` | Google Sheet ID, Service Account Email, API keys |
| `backend/.env` | PORT, AI_SERVICE_URL, Google config |
| `backend/config/service-account-key.json` | Google Service Account private key |

**Backup location**: `~/Projects/mia-warehouse-management-v2-backup-20260312/`

---

*Tạo bởi: Copilot — 12/03/2026 23:50*
